<div>
<%= notice %>

<h1>Editing Book</h1>
<%= form_with model:@book, url:"/books/#{@book.id}", local:true do |f| %><br>

<% if @book.errors.any? %>
<%= @book.errors.count %>errors prohibited this book from being saved:<br>
<% @book.errors.full_messages.each do |message| %>
<%= message %><br>
<% end %>
<% end %>

<label>Title</label>
<%= f.text_field :title %><br>
<br>
<label>Body</label>
<%= f.text_area :body %><br>
<br>
<%= f.submit 'Update Book' %><br>
<% end %>
<br>
<%= link_to 'show', book_path(id: @book.id) %>
|
<%= link_to 'back', books_path %>

<div>